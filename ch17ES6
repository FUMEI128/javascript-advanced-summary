第十五周学习
ES6
一，变量声明
let 只作用于块作用域
特点：
1，禁止重复声明
2，不存在变量提升
3，暂时性死区（不受外部的影响）

const命令声明一个只读的常量，该值是不可以改变的。

二，默认值
ES6允许为函数的参数设置默认值，直接卸载参数定义的后面

三，解构赋值
对象的解构赋值：先找到同名的属性，然后再赋值给对应的变量，对象的属性是没有顺序的

数组的解构赋值：数组的元素是按次序排列的，变量的取值由它的位置决定
用法：可以互换两个变量的值

函数解构：

四，拓展运算符

1，...将数组或类数组对象展开，将数组中的数给提取出来
2，res运算符 将值合成一个数组

五，模板字符串
是增强版的字符串，用``表示 在字符串中放入变量，需要将变量名写在$()中

字符串的方法：includes()表示是否找到参数字符串
                       startsWith()参数是否在字符串的头部位置
                       endWith()参数是否在原字符串的尾部




