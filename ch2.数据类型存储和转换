数据类型存储和转换

一，数据类型分类：
引用数据类型：Object (Math,Array)
基本数据类型：Number String Boolean Null Undefined
二，数据类型存储：
1，基本类型的变量存放在栈区
     基本类型的值是不可改变的
2，引用类型是在栈内存中存放变量的地址 堆内存里面存放变量的值

3，基本类型与引用类型的区别
1）访问机制不同，基本数据类型是直接访问，引用数据类型先从栈区中获得地址，从堆区中取得值
2）复制变量不同（赋值运算的过程），基本类型相互不影响，引用类型会受到影响
3）比较变量不同，比较的过程，基本数据类型比较的是值是否相等，引用数据类型比较的是它们的地址是否相同
4）参数传递不同，在用函数传参的时候，基本数据类型是按值传参，引用类型是将对象的引用值（地址）传递。

三，数据类型转换
1，转换为Number类型规则
undefined--NaN
null--0
false --0,true--1

2,转换为String类型规则
undefined--"undefined"
null--"null"

3.转换为Boolean类型规则
undefined--False
null--False
0,NaN--False
