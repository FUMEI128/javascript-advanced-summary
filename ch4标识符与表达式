标识符与表达式

一，标识符
二，表达式与运算符

三，语句
1，运算符的优先级：逗号运算符的优先级最低，等号是倒数第二
2，函数声明与函数表达式的区别
只要在function关键字的前面有任何的其他元素则为函数表达式。

3，逻辑运算符（重点）
当逻辑运算符&&和||两侧的操作数不是布尔类型的时候
短路原则：
1）对于&&（防止运行报错）,左操作数为true，返回右操作数
                   左操作数为false，返回左操作数
2）对于||（适用于设置参数的默认值）. 左操作数为true，返回左操作数
                左操作数为flase ，返回右操作数。

4，相等运算符
==比较的是数值是否相同
===比较的是两个数类型相同，且值相等
5，逗号运算符
6，复合语句，多条语句联合在一起，用花括号将多条语句括起来--语句块
语句是没有返回值的代码
表达式是有表达式的的代码。
